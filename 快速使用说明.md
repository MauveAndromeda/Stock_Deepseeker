# Stock_Deepseeker 快速使用说明

## 🚀 5秒钟开始回测

```bash
# 1. 下载项目（任选一种方式）
# 方式1：下载ZIP并解压
# 方式2：git clone

# 2. 进入目录
cd Stock_Deepseeker

# 3. 运行回测（仅此一条命令！）
python 一键回测.py
```

**就这么简单！** 脚本会自动：
- ✅ 创建虚拟环境
- ✅ 安装所有依赖
- ✅ 配置API密钥
- ✅ 运行回测

---

## 📖 详细说明

### 第一次运行（约10分钟）

```bash
# 使用默认配置（Fast模式，3年数据）
python 一键回测.py

# 快速验证（Turbo模式，1年数据）
python 一键回测.py --mode turbo --years 1

# 查看帮助
python 一键回测.py --help
```

**第一次运行会：**
1. 创建虚拟环境（`.venv/` 目录）
2. 安装依赖包（约5-10分钟）
3. 提示输入OpenAI API密钥（可选）
4. 开始回测

### 后续运行（快速）

```bash
# 直接运行，无需等待安装
python 一键回测.py --mode fast
```

**后续运行只需：**
- 直接使用已有虚拟环境
- 跳过依赖安装
- 立即开始回测

---

## 🎯 4种回测模式

| 模式 | 命令 | 耗时 | 适用场景 |
|------|------|------|----------|
| **Turbo** | `--mode turbo` | 3-5分钟 | 快速验证想法 |
| **Fast** ⭐ | `--mode fast` | 10-15分钟 | 日常开发（推荐） |
| **Balanced** | `--mode balanced` | 30-60分钟 | 正式评估 |
| **Full** | `--mode full` | 2-4小时 | 学术研究 |

---

## 📝 使用示例

```bash
# 示例1：快速验证（推荐新手）
python 一键回测.py --mode turbo --years 1

# 示例2：日常回测（推荐）
python 一键回测.py --mode fast --years 3

# 示例3：自定义股票池
python 一键回测.py --mode fast --symbols AAPL MSFT GOOGL NVDA META

# 示例4：长期回测
python 一键回测.py --mode balanced --years 5

# 示例5：完整回测（最高准确性）
python 一键回测.py --mode full --years 5
```

---

## 🔧 API密钥配置

### 方法1：交互式输入（推荐新手）

首次运行时，脚本会提示输入API密钥。

### 方法2：创建.env文件（推荐）

在项目根目录创建 `.env` 文件：

```bash
OPENAI_API_KEY=sk-your-key-here
```

### 方法3：环境变量

```bash
# Linux/Mac
export OPENAI_API_KEY=sk-your-key-here
python 一键回测.py

# Windows CMD
set OPENAI_API_KEY=sk-your-key-here
python 一键回测.py

# Windows PowerShell
$env:OPENAI_API_KEY="sk-your-key-here"
python 一键回测.py
```

---

## 📊 查看结果

回测完成后，报告保存在：

```
backtest_reports/
├── report_20251109_153045.json  # 最新报告
├── report_20251108_142301.json
└── ...
```

**查看报告：**

```bash
# Linux/Mac
cat backtest_reports/report_*.json | tail -1 | jq

# Windows
type backtest_reports\report_*.json
```

**报告内容：**
- 收益指标：总收益、年化收益、Sharpe、Sortino
- 风险指标：最大回撤、VaR、CVaR、波动率
- 交易指标：胜率、盈亏比、交易次数
- 持仓分析：平均仓位、集中度、换手率

---

## 🛠️ 常见问题

### Q1: Python版本要求？
**A**: Python 3.10 或更高版本

```bash
# 检查版本
python --version

# 或
python3 --version
```

### Q2: 首次安装太慢？
**A**: 依赖安装需要5-10分钟，请耐心等待。后续运行会很快。

### Q3: 如何重新安装依赖？
**A**:
```bash
# 方法1：强制重装
python 一键回测.py --force-install

# 方法2：删除虚拟环境重来
rm -rf .venv
python 一键回测.py
```

### Q4: 在Windows上运行慢？
**A**:
1. 使用 Python 官方版本（不是Microsoft Store版本）
2. 关闭杀毒软件的实时扫描
3. 使用 `--mode turbo` 快速验证

### Q5: 没有API密钥怎么办？
**A**:
1. 注册OpenAI账号: https://platform.openai.com/
2. 创建API密钥: https://platform.openai.com/api-keys
3. 或跳过AI功能（回测仍可运行，但缺少AI分析）

### Q6: 如何清理缓存？
**A**:
```bash
# 清理数据缓存
rm -rf data_cache/

# 清理回测报告
rm -rf backtest_reports/

# 完全清理
rm -rf .venv data_cache/ backtest_reports/
```

---

## 📁 项目结构

```
Stock_Deepseeker/
├── 一键回测.py                 # ⭐ 主脚本（从这里开始）
├── 快速使用说明.md              # 本文档
├── README.md                    # 详细文档
├── .venv/                       # 虚拟环境（自动创建）
├── .env                         # API密钥（自动创建）
├── configs/                     # 配置文件
├── docs/                        # 详细文档
├── scripts/                     # 内部脚本
├── src/                         # 源代码
├── backtest_reports/            # 回测报告（自动创建）
└── data_cache/                  # 数据缓存（自动创建）
```

---

## 🎓 进阶使用

### 批量回测

```bash
# 测试多个模式
for mode in turbo fast balanced; do
  echo "Testing $mode mode..."
  python 一键回测.py --mode $mode --years 2
done
```

### 参数优化

```bash
# 测试不同时间周期
for years in 1 2 3 5; do
  python 一键回测.py --mode turbo --years $years
done
```

### 股票池测试

```bash
# 科技股
python 一键回测.py --symbols AAPL MSFT GOOGL NVDA META TSLA

# 金融股
python 一键回测.py --symbols JPM BAC GS MS C WFC

# 医疗股
python 一键回测.py --symbols UNH JNJ PFE ABBV TMO
```

---

## 📚 完整文档

- [详细使用指南](docs/guides/ultimate-backtest-guide.md)
- [快速开始](docs/quickstart.md)
- [系统架构](docs/multi-agent-system.md)

---

## ✨ 核心特性

- ✅ **100%功能覆盖**：Multi-Agent + 14策略 + 因子 + 风险管理
- ⚡ **极致性能**：并行处理，最高256x加速
- 🎯 **严格准确**：100%时间安全，无前视偏差
- 🔧 **零配置**：自动创建环境，自动安装依赖
- 📦 **完全独立**：下载即用，无需预配置

---

## 🆘 获取帮助

```bash
# 查看所有选项
python 一键回测.py --help

# 查看详细文档
cat docs/guides/ultimate-backtest-guide.md

# 提交问题
https://github.com/MauveAndromeda/Stock_Deepseeker/issues
```

---

**开始你的量化交易之旅！** 🚀
