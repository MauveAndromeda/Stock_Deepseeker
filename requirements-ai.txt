# Optional AI/LLM dependencies (only installed with --ai flag)
# These are NOT required for basic backtesting

# AI API Clients
openai>=1.0.0
anthropic>=0.18.0

# LangChain (pinned to avoid langsmith/zstandard DLL issues)
langchain==0.3.9
langchain-core==0.3.11
langchain-openai==0.2.2
langchain-anthropic==0.2.1

# Async Support
aiohttp>=3.9.0
nest-asyncio>=1.6.0
httpx>=0.27.0

# Additional ML (optional)
scikit-learn>=1.3.0
scipy>=1.11.0
hmmlearn>=0.3.0

# Note: langsmith and zstandard are intentionally excluded
# to avoid Windows DLL loading issues with long paths.
# If LangChain pulls them transitively, imports are guarded.
